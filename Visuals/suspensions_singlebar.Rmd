```{r}
source("./visual_fx.R")

con_ctc<- connect_to_db("cancel_the_contract")

df<-dbGetQuery(con_ctc, "SELECT * FROM analysis_suspensions")

indicator <- "Suspensions by race"
title_text <- "AVUHSD administrators suspend nearly 1 in 5 Black students"

single_bar_tot(df, indicator, title_text)

dbDisconnect(con_ctc)
```
