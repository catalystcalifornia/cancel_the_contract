```{r}
source("./visual_fx.R")

con_ctc<- connect_to_db("cancel_the_contract")

df<-dbGetQuery(con_ctc, "SELECT race as label, rate_own FROM analysis_homeowners_renters_race") 

df_subset <- df %>%
  race_sp(.)

indicator <- "Home ownership"

single_bar_tot_sp_v2(df_subset, indicator)

dbDisconnect(con_ctc)
```

